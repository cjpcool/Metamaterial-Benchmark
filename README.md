# The LLM part (crystal-text-llm and CrystaLLM) of the Benchmark Projects

In each folder, you could find the commands.txt file, which contains the commands to run the code.

The environment is specified in the environment.yml file.
