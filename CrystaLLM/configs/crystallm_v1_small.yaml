out_dir: '/home/zhang/Metamaterial-Benchmark/CrystaLLM/metamat_crystallm_v1_small_feb12'
eval_interval: 100  # how often to evaluate against the validation set
eval_iters_train: 100
log_interval: 1  # how often to print to the console (1 = every iteration)
init_from: 'scratch'

# whether to always save a checkpoint
always_save_checkpoint: True

# whether to validate with a validation set
validate: False

dataset: '/home/zhang/Metamaterial-Benchmark/CrystaLLM/metamat_tokens_v1_train'
batch_size: 128
block_size: 1024

n_layer: 8
n_head: 8
n_embd: 512
dropout: 0.1

learning_rate: 1e-3
max_iters: 2000
lr_decay_iters: 2000
min_lr: 1e-4
beta2: 0.99

warmup_iters: 100
